name: kuairand
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - lightgbm
      - tqdm
      - jupyterlab
      - matplotlib
      - pyarrow
      - joblib
      - pytest